---
/**
 * Global nav / contact micro-animations for LifeLoggerz
 */
---
<style is:global>
  /* Contact button pulse */
  @keyframes lifeloggerzContactPulse {
    0% {
      transform: scale(1);
      box-shadow: 0 0 0px rgba(37, 99, 235, 0.0);
    }
    40% {
      transform: scale(1.05);
      box-shadow: 0 0 14px rgba(37, 99, 235, 0.4);
    }
    100% {
      transform: scale(1);
      box-shadow: 0 0 0px rgba(37, 99, 235, 0.0);
    }
  }

  /* TEMP: no delay so we can *see* it immediately */
  .contact-button a.contact-pulse {
    animation: lifeloggerzContactPulse 1.2s ease-in-out 0s infinite;
    transform-origin: center;
  }

  /* Respect reduced-motion users */
  @media (prefers-reduced-motion: reduce) {
    .contact-button a.contact-pulse {
      animation: none !important;
      box-shadow: none !important;
      transform: none !important;
    }
  }
</style>
