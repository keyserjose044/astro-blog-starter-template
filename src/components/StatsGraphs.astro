<section id="graphs" class="dashboard-section graph-explorer" aria-labelledby="graphs-heading">
  <div class="section-heading">
    <p class="eyebrow">Interactive archive explorer</p>
    <h2 id="graphs-heading">See how the habits changed</h2>
    <p>
      Explore daily, weekly, monthly, and cumulative values calculated from the same public-safe daily archive.
    </p>
  </div>
</section>

<section id="hobby-time" class="dashboard-section hobby-section" aria-labelledby="hobby-heading">
  <div class="section-heading section-heading--split">
    <div>
      <p class="eyebrow">One timeline, several priorities</p>
      <h2 id="hobby-heading">Where my hobby time went</h2>
    </div>
    <p>
      Compare selected activities on one hour scale using stacked bars or shape-preserving curves.
    </p>
  </div>
</section>

<section id="correlations" class="dashboard-section correlation-section" aria-labelledby="correlation-heading">
  <div class="section-heading section-heading--split">
    <div>
      <p class="eyebrow">Relationships between habits</p>
      <h2 id="correlation-heading">Correlation explorer</h2>
    </div>
    <p>
      Compare daily or monthly observations. Correlation can reveal patterns worth investigating, but it does not establish causation.
    </p>
  </div>
</section>
